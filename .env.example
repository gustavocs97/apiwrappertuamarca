# ---------------------------------------
# Arquivo de exemplo para configuração
# ---------------------------------------
# Preencha as variáveis abaixo com os valores adequados para o seu ambiente

# Configurações do wrapper (para áudio, imagem e novos wrappers)
# Chave para autenticar o wrapper (substitua pelo seu valor real)
API_KEY_OFWRAPPER=your_wrapper_api_key_here

# Porta para a execução da aplicação
# A porta que a aplicação será exposta no container (por padrão, 3001)
PORT=3001

# Nível de log
# Ajuste o nível de log para um dos seguintes valores: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=DEBUG

# ---------------------------------------
# Configurações da API de áudio
# ---------------------------------------

# URL da API original de áudio (substitua pelo seu valor real)
ORIGINAL_API_URL=http://example.com/api/audio

# Chave para a API de áudio original (substitua pelo seu valor real)
API_KEY=your_audio_api_key_here

# Caminho para o diretório de armazenamento de arquivos de áudio
# O caminho onde os arquivos de áudio serão armazenados no container
STORAGE_PATH=/app/temp_audio

# Tempo máximo para armazenar arquivos de áudio (em segundos)
# O tempo após o qual o arquivo será removido (exemplo: 120 segundos)
MAX_STORAGE_TIME=120

# ---------------------------------------
# Configurações da API de imagem
# ---------------------------------------

# URL da API de imagem (substitua pelo seu valor real)
IMAGE_API_URL=https://example.com/api/image

# Chave para a API de imagem (substitua pelo seu valor real)
IMAGE_API_KEY=your_image_api_key_here

# Caminho para o diretório de armazenamento de imagens geradas
# O caminho onde as imagens geradas serão armazenadas no container
IMAGE_STORAGE_PATH=/app/temp_images

# Tempo máximo para armazenar imagens (em segundos)
# O tempo após o qual a imagem será removida (exemplo: 300 segundos)
MAX_IMAGE_STORAGE_TIME=300

# ---------------------------------------
# Configurações do novo wrapper (opcional, comentado)
# ---------------------------------------

# URL da nova API (substitua pelo seu valor real, caso vá utilizar um novo wrapper)
# NOVO_WRAPPER_API_URL=https://example.com/api/newwrapper

# Chave de autenticação para o novo wrapper (substitua pelo seu valor real, caso vá utilizar)
# NOVO_WRAPPER_API_KEY=your_new_api_key_here

# Caminho de armazenamento temporário para o novo wrapper
# Caminho onde arquivos temporários para o novo wrapper serão armazenados
# NOVO_STORAGE_PATH=/app/temp_newwrapper_exemplo





# Como usar o arquivo .env
#1. Copie este arquivo para .env:
#   cp .env.example .env
#
#2. Preencha as variáveis de ambiente com os valores apropriados para o seu ambiente. Você pode usar valores específicos para produção, desenvolvimento ou testes.
#
#3. Não compartilhe o arquivo .env com informações sensíveis em repositórios públicos! Mantenha-o seguro e fora do controle de versão.
#
#4. O Docker Compose irá automaticamente carregar as variáveis do arquivo .env quando você executar o `docker-compose up`.

